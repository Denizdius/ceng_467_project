# ── Core ML / training ──────────────────────────────────────────────────────
torch==2.7.0
transformers==4.57.3
datasets==4.3.0
accelerate==1.4.0
peft==0.15.0
trl==0.22.1
bitsandbytes==0.49.0        # 4-bit quantisation (QLoRA backbone weights)

# ── Unsloth (faster LoRA kernels) ────────────────────────────────────────────
unsloth==2025.12.9

# ── Inference / serving ──────────────────────────────────────────────────────
vllm==0.9.2                 # PagedAttention engine (eval + inference scripts)

# ── Evaluation harness ───────────────────────────────────────────────────────
lm_eval==0.4.8              # lm-evaluation-harness (MMLU, ARC-Challenge)

# ── GPU monitoring ───────────────────────────────────────────────────────────
pynvml==12.0.0              # NVML bindings used by gpu_metrics_logger.py

# ── Plotting / reporting ─────────────────────────────────────────────────────
matplotlib==3.9.2           # training curves and GPU metric plots

# ── Notes ────────────────────────────────────────────────────────────────────
# Install in a dedicated venv (Python 3.11 recommended):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# vllm requires CUDA 12.x and is Linux-only.
# unsloth must be installed AFTER torch; see https://github.com/unslothai/unsloth
# for any platform-specific wheel instructions.
